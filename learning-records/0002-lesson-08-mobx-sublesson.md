# Lesson 08 gets a MobX sub-lesson

User asked to switch the planned “Lifting state up — filters actually filter, sort actually sorts” lesson so it also includes a MobX path. Curriculum is now **08a** (React lift + derived filtered/sorted list) then **08b** (same behaviour via a MobX store + `observer` / `computed`), as the bridge to Rides SDK filter stores.

## Implications
- Do not author or teach **08b** before the learner can complete **08a**
- Mission stack remains Next.js + TypeScript + Emotion; MobX is transfer learning, not the default page stack unless the mission is later revised with the user
