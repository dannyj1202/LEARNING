# Prior knowledge: some HTML, CSS, and JavaScript

User reports partial familiarity with HTML, CSS, and JS, and no stated React experience. Future lessons must not assume React mental models (components, props, state, hooks) but can use basic DOM/layout vocabulary. Depth of JS (closures, modules, async) is unknown — probe lightly before relying on them.

## Implications
- First lessons map UI pieces from the vehicle page onto React components
- Defer Next.js App Router, TypeScript generics, and Emotion until component/props/state floor is solid
